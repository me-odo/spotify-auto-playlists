"""Pydantic schemas for the auth API.

This module is reserved for authentication-related request/response models.
At the moment the auth routes only return plain dict responses, so no
BaseModel schemas are defined yet.
"""
